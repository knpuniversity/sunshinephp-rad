{% extends '::base.html.twig' %}

{% block body %}

{{ form_start(form) }}
    {{ form_widget(form, {
        'label': 'Create team'
    }) }}
    <input type="submit" class="btn" />
{{ form_end(form) }}

{% endblock body %}