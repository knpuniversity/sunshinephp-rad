{% extends '::base.html.twig' %}

{% block body %}

{{ form_start(form, { 'attr': {'class': 'form form-horizontal'} }) }}
    {{ form_widget(form, {
        'label': 'Edit team',
    }) }}
    <input type="submit" class="btn" />
{{ form_end(form) }}

{% endblock body %}